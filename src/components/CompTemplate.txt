import React from 'react';
import { connect } from 'react-redux';
import PropTypes from 'prop-types';
import { someActions } from '../actions';

class AppCont extends React.Component {
    constructor(props) {
        super(props);
    }
    
    componentDidMount() {
    }
    
    render() {
        return [];
    }
}


AppCont.propTypes = {
    action: PropTypes.func.isRequired,
    appCompState: PropTypes.object.isRequired
}

const mapStateToProps = (state) => ({
    appCompState: state.appCompState
})

export default connect(mapStateToProps, { action: someActions.action })(AppComp);