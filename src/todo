finish implementing cards
add scrollbar component
improve error handling in actions
convert contentState.hoverDisabled to map
work on color schemes

need workaround for touch controls when editing on mobile

add component schema to help input validation?
put redux store in web worker?